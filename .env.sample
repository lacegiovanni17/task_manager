# Server Port
PORT=4404

# MongoDB Connection String (replace with your own credentials)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.wtwujyr.mongodb.net/

# JWT Secret Key (use a strong random value)
JWT_SECRET=your_super_secret_key
